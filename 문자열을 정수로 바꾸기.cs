using System;

public class Solution {
    public int solution(string s) {
        
        int number = Int32.Parse(s);

        return number;
        
        //int answer = 0;
        //return answer;
    }
}